"""Native equivalents of the repository's local bbcode/html2text fallbacks."""


def bbcode_format(text: String) -> String:
    return text


def render_bbcode_html(text: String) -> String:
    return text


def html_to_text(text: String) -> String:
    return text
